# AI Wonderland Backend

Minimal Node.js/Express backend scaffold.

## Features
- Health check endpoint (`/health`)
- Security best practices (`helmet`, `cors`, environment variables)
- Ready for API endpoints, authentication, validation, and database integration

## Getting Started

1. Copy `.env.example` to `.env` and set your secrets.
2. Install dependencies: `npm install`
3. Run the server: `npm start`
